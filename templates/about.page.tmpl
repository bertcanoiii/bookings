{{template "base" .}}

{{define "content"}}

    <div class="container">
        <div class="row">
            <div class="col">
                <h1>This is the about page</h1>
                <p>This was added after setting UseCache to false</p>
            </div>
        </div>
    </div>

{{end}}